<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fivekm_home.charge.mapper.CSMapper">
    <insert id="csReg">
        insert into CS values(
        #{chargeName},
        #{operation},
        #{chargeSpeed},
        #{chargeAmount},
        #{min30Fee},
        #{addMin10Fee},
        #{manageTime},
        #{chargePic},
        #{aptMap},
        #{cable},
        #{chargeType},
        sysdate,
        'N',
        #{resName}
        )
    </insert>
    <select id="csRequestList" resultType="com.fivekm_home.charge.domain.CS.CS_requestList">
        SELECT CHARGENAME, OPERATION, CHARGESPEED, CHARGEAMOUNT, MIN30FEE, ADDMIN10FEE, CABLE, CHARGETYPE, CHARGINGCHK
        FROM CS
    </select>
    <select id="csRequestPick" resultType="com.fivekm_home.charge.domain.CS.CS_requestPick">
        SELECT CHARGENAME, OPERATION, CHARGESPEED, CHARGEAMOUNT,MIN30FEE, ADDMIN10FEE, CABLE, CHARGETYPE, CHARGINGCHK
        FROM CS WHERE CHARGENAME = #{csName}
    </select>

    <update id="updateChargingChk">
        update cs_view
        set chargingChk = 'Y'
        where chargeName = #{chargeName}
    </update>
</mapper>