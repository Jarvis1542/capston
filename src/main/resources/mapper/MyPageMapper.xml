<?xml version="1.0" encoding="UTF-8"?> <!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fivekm_home.charge.mapper.MyPageMapper">
    <update id="updatePassword">
        update member_view set password = #{password}
        where email = #{email}
    </update>

    <insert id="regGuard">
        insert into guard_view values (
            #{email},
            #{guardLicence},
            #{guardCo},
            #{guardCoNum},
            sysdate,
            null
        )
    </insert>

    <insert id="regRegister">
        insert into register_view values (
            #{email},
            #{registerLicence},
            #{accountNum},
            #{bankName},
            sysdate
        )
    </insert>

    <select id="checkResidence" resultType="int">
        select count(*) from residence
        where resName = #{resName}
    </select>

    <insert id="regResidence">
        insert into residence_view values (
            #{resName},
            #{postcode},
            #{roadAddress},
            #{jibunAddress},
            #{extraAddress},
            #{detailAddress},
            #{lat},
            #{lng},
            sysdate,
            #{email}
        )
    </insert>

    <select id="userHpBookmark" resultType="com.fivekm_home.charge.domain.USER.UserHpBookmark">
        select parkingPic, parkingName, place, min30Fee, addmin10Fee, aptMap,
        roadAddress, extraAddress, name, phone, manageTime, m.email
        from member_view m, parking_view p, residence_view r
        where m.email = r.email
        and p.resName = r.resName
        and m.email = #{email}
        and parkingChk = (select parkingChk
                            from parking_view
                            where parkingChk = 'Y')
        and parkingName =
    </select>
</mapper>