1. 서버를 terminate 한다.

2. shift를 두 번 눌러서 ChargeApplication.java 검색하고

    ChargeApplication.java에서

    @Bean
    CommandLineRunner init(StorageService storageService) {
        return (args) -> {
            storageService.deleteAll();
            storageService.init();
        };
    }

    이것들의 주석을 해제한다.

2. /resources/static/img/upload 파일 우클릭 - find in path... 클릭
    scope 오른쪽에 주소 복사

3. application.properties의 file.location= 여기에 붙여넣기한다. (띄어쓰기 있으면 안됨)
ex) file.location=D:\\Backup\\F\\Java_Workspace\\charge\\src\\main\\resources\\static\\img\\upload\\

4. 5KmHC_삭제.sql에서 ctrl+A 눌러서 복사하고 sql developer에서 붙여넣기해서 다 삭제한다.

5. 5KmHC_생성.sql에서 ctrl+A 눌러서 복사하고 sql developer에서 붙여넣기해서 다 생성한다.

6. ChargeApplication.java에서

       @Bean
       CommandLineRunner init(StorageService storageService) {
           return (args) -> {
               storageService.deleteAll();
               storageService.init();
           };
       }

       이것들을 주석 처리한다.

7. 서버 실행

※※※
spring.jpa.hibernate.ddl-auto=update --> update를 create 바꿀 필요 없음.